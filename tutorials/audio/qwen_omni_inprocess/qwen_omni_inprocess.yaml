# Copyright (c) 2026, NVIDIA CORPORATION.  All rights reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# Granary v2 - Qwen-Omni In-Process Pipeline (Hydra Config)
#
# Small PR first-stage pipeline:
#   NemoTarredAudioReader -> ASRStage (Qwen-Omni adapter) -> ShardedManifestWriter
#
# The middle stage uses a generic ASR stage with a pluggable adapter:
# ``ASRStage`` owns Curator-side glue (task.data reads, validate_input,
# batching, ISO-code -> language-name lookup, schema mutation, metrics);
# Qwen-Omni-specific knobs live under ``adapter_kwargs``. Swap models by
# replacing the single ``adapter_target:`` line.
#
# Invoked directly or by an external launcher via:
#   python tutorials/audio/qwen_omni_inprocess/main.py \
#       --config-path=<path> --config-name=qwen_omni_inprocess \
#       workspace_dir=/work input_manifest=/data/config.yaml
#
# For tarred data, `input_manifest` should point to the Granary YAML data config.

defaults:
  - _self_
  - override hydra/job_logging: none
  - override hydra/hydra_logging: none

hydra:
  run:
    dir: .
  output_subdir: null

# ===========================================================================
# CORE - these may be overridden at runtime by the launcher
# ===========================================================================
input_manifest: ???
workspace_dir: /local/qwen_omni_workspace
final_manifest: ${workspace_dir}/output.jsonl
language_short: en
max_segment_length: 40
backend: ray_data
# Xenna-only; ignored by the default ray_data backend.
execution_mode: streaming
autoscale_interval_s: 30
# When true, ASRStage.setup_on_node raises if the adapter's prefetch_weights fails.
prefetch_fail_on_error: true
stages_to_run: all
stages_to_skip: []

# data_config defaults to input_manifest.
data_config: ${input_manifest}
output_dir: ${workspace_dir}
corpus: null
max_utterances_per_shard: null
source_lang_key: source_lang
cpu_batch_size: 64
duration_key: duration

# ASR adapter.
adapter_target: nemo_curator.models.asr.QwenOmniASRAdapter
model_id: "Qwen/Qwen3-Omni-30B-A3B-Instruct"
revision: null

pred_text_key: qwen3_prediction_s1
disfluency_text_key: qwen3_prediction_s2

# Qwen-Omni chunking is model-safety slicing. Duration-aware bucketing below is
# the optional GPU-dispatch scheduler that mixes those chunks by cost.
chunking:
  enabled: true
  ideal_inference_segment_s: 2400
  max_inference_duration_s: 2400

# Backward-compatible flat aliases for existing Hydra overrides.
ideal_inference_segment_s: ${chunking.ideal_inference_segment_s}
max_inference_duration_s: ${chunking.max_inference_duration_s}

keep_waveform: true

# Worker pin. Set both null to use backend autoscaling.
asr_num_workers_per_node: null
asr_num_workers: 4

ml_prompt: "Transcribe the audio."
ml_prompt_file: null
en_prompt_file: null
followup_prompt: null
followup_prompt_file: null
system_prompt: null
tensor_parallel_size: 2
omni_resource_gpus: 2.0
# Candidate row window used when duration-aware bucketing is disabled, and as
# the fallback if a policy cannot derive a planner window. It is not the GPU
# dispatch width when duration-aware bucketing is enabled; batch_policy caps
# control that.
candidate_batch_size: 32
model_batch_size: ${candidate_batch_size}
max_output_tokens: 256
max_model_len: 4096
max_num_batched_tokens: null
max_num_seqs: 16
gpu_memory_utilization: 0.90
prep_workers: 16

# vLLM runtime knobs.
enable_prefix_caching: true
prefix_caching_hash_algo: xxhash
limit_mm_per_prompt_audio: 2
seed: 1234

# Duration-aware bucketing only controls scheduler mixing. Set `enabled=false`
# to keep normal backend batch flow while preserving chunking when
# `chunking.enabled=true`.
duration_aware_bucketing:
  enabled: true
  strategy: duration_bucketed
  buckets_sec: [0, 600, 1200, 2400]
  max_items_per_batch_by_bucket: [32, 16, 8, 4]
  max_audio_sec_per_batch: 2400
  # null preserves the default sum(max_items_per_batch_by_bucket) candidate window.
  prebatching_window_size: null
  flush_interval_ms: 250

# Stage graph.
stages:
  - stage_id: reader
    _target_: nemo_curator.stages.audio.io.nemo_tarred_reader.NemoTarredAudioReader
    yaml_path: ${data_config}
    corpus_filter: ${corpus}
    output_dir: ${output_dir}
    duration_key: ${duration_key}
    max_duration_s: ${max_segment_length}
    max_utterances_per_shard: ${max_utterances_per_shard}
    stage_with:
      nemo_tar_shard_reader:
        resources:
          _target_: nemo_curator.stages.resources.Resources
          cpus: 4.0

  - stage_id: qwen_omni
    _target_: nemo_curator.stages.audio.inference.asr.ASRStage
    name: QwenOmni_inference
    adapter_target: ${adapter_target}
    model_id: ${model_id}
    revision: ${revision}
    source_lang_key: ${source_lang_key}
    default_language: ${language_short}
    pred_text_key: ${pred_text_key}
    disfluency_text_key: ${disfluency_text_key}
    chunking_enabled: ${chunking.enabled}
    ideal_inference_segment_s: ${ideal_inference_segment_s}
    max_inference_duration_s: ${max_inference_duration_s}
    keep_waveform: ${keep_waveform}
    prefetch_fail_on_error: ${prefetch_fail_on_error}
    xenna_num_workers_per_node: ${asr_num_workers_per_node}
    xenna_num_workers: ${asr_num_workers}
    # ASRStage field remains `batch_size`; this is the direct model-call cap.
    # Duration-aware mode should set bucket caps no larger than this for the
    # target model/GPU shape.
    batch_size: ${model_batch_size}
    batch_policy:
      _target_: nemo_curator.stages.audio.inference.batch_policy.BatchPolicy
      enabled: ${duration_aware_bucketing.enabled}
      strategy: ${duration_aware_bucketing.strategy}
      buckets_sec: ${duration_aware_bucketing.buckets_sec}
      max_items_per_batch_by_bucket: ${duration_aware_bucketing.max_items_per_batch_by_bucket}
      max_audio_sec_per_batch: ${duration_aware_bucketing.max_audio_sec_per_batch}
      prebatching_window_size: ${duration_aware_bucketing.prebatching_window_size}
      flush_interval_ms: ${duration_aware_bucketing.flush_interval_ms}
    adapter_kwargs:
      prompt_text: ${ml_prompt}
      prompt_file: ${ml_prompt_file}
      en_prompt_file: ${en_prompt_file}
      followup_prompt: ${followup_prompt}
      followup_prompt_file: ${followup_prompt_file}
      system_prompt: ${system_prompt}
      tensor_parallel_size: ${tensor_parallel_size}
      max_output_tokens: ${max_output_tokens}
      max_model_len: ${max_model_len}
      max_num_batched_tokens: ${max_num_batched_tokens}
      max_num_seqs: ${max_num_seqs}
      gpu_memory_utilization: ${gpu_memory_utilization}
      prep_workers: ${prep_workers}
      enable_prefix_caching: ${enable_prefix_caching}
      prefix_caching_hash_algo: ${prefix_caching_hash_algo}
      limit_mm_per_prompt_audio: ${limit_mm_per_prompt_audio}
      seed: ${seed}
    stage_with:
      resources:
        _target_: nemo_curator.stages.resources.Resources
        gpus: ${omni_resource_gpus}

  - stage_id: sharded_manifest_writer
    _target_: nemo_curator.stages.audio.io.sharded_manifest_writer.ShardedManifestWriterStage
    output_dir: ${output_dir}
    final_manifest_path: ${final_manifest}
    duration_key: ${duration_key}
    stage_with:
      batch_size: ${cpu_batch_size}
