---
layout: overview
slug: nemo-curator/nemo_curator/stages/text/modules/add_id
title: nemo_curator.stages.text.modules.add_id
---

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`AddId`](#nemo_curator-stages-text-modules-add_id-AddId) | The module responsible for adding unique IDs to each document record. |

### API

<Anchor id="nemo_curator-stages-text-modules-add_id-AddId">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class nemo_curator.stages.text.modules.add_id.AddId(
    id_field: str,
    id_prefix: str | None = None,
    overwrite: bool = False,
    name: str = 'add_id'
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [ProcessingStage[DocumentBatch, DocumentBatch]](/nemo-curator/nemo_curator/stages/base#nemo_curator-stages-base-ProcessingStage)

The module responsible for adding unique IDs to each document record.

This stage adds a unique identifier to each document in the batch by combining
the task UUID with a sequential index.

**Parameters:**

<ParamField path="id_field" type="str">
The field where the generated ID will be stored.
</ParamField>

<ParamField path="id_prefix" type="str | None" default="None">
A prefix to add to the generated IDs.
</ParamField>

<ParamField path="overwrite" type="bool" default="False">
Whether to overwrite existing IDs.
</ParamField>


<ParamField path="id_field" type="str">
</ParamField>

<ParamField path="id_prefix" type="str | None = None">
</ParamField>

<ParamField path="name" type="str = 'add_id'">
</ParamField>

<ParamField path="overwrite" type="bool = False">
</ParamField>
<Anchor id="nemo_curator-stages-text-modules-add_id-AddId-inputs">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.text.modules.add_id.AddId.inputs() -> tuple[list[str], list[str]]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="nemo_curator-stages-text-modules-add_id-AddId-outputs">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.text.modules.add_id.AddId.outputs() -> tuple[list[str], list[str]]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="nemo_curator-stages-text-modules-add_id-AddId-process">

<CodeBlock links={{"nemo_curator.tasks.DocumentBatch":"/nemo-curator/nemo_curator/tasks/document#nemo_curator-tasks-document-DocumentBatch"}} showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.text.modules.add_id.AddId.process(
    batch: nemo_curator.tasks.DocumentBatch
) -> nemo_curator.tasks.DocumentBatch | None
```

</CodeBlock>
</Anchor>

<Indent>

Adds unique IDs to each document in the batch.

The IDs are generated by combining the batch UUID with a sequential index,
ensuring uniqueness across the entire dataset.

**Parameters:**

<ParamField path="batch" type="DocumentBatch">
The batch to add IDs to
</ParamField>

**Returns:** `DocumentBatch | None`

A batch with unique IDs added to each document


</Indent>
</Indent>
