---
layout: overview
slug: nemo-curator/nemo_curator/stages/text/models/tokenizer
title: nemo_curator.stages.text.models.tokenizer
---

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`TokenizerStage`](#nemo_curator-stages-text-models-tokenizer-TokenizerStage) | Tokenizer stage for Hugging Face models. |

### API

<Anchor id="nemo_curator-stages-text-models-tokenizer-TokenizerStage">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class nemo_curator.stages.text.models.tokenizer.TokenizerStage(
    model_identifier: str,
    cache_dir: str | None = None,
    hf_token: str | None = None,
    text_field: str = 'text',
    max_chars: int | None = None,
    max_seq_length: int | None = None,
    padding_side: typing.Literal['left', 'right'] = 'right',
    sort_by_length: bool = True,
    unk_token: bool = False
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [ProcessingStage[DocumentBatch, DocumentBatch]](/nemo-curator/nemo_curator/stages/base#nemo_curator-stages-base-ProcessingStage)

Tokenizer stage for Hugging Face models.

**Parameters:**

<ParamField path="model_identifier" type="str">
The identifier of the Hugging Face model.
</ParamField>

<ParamField path="cache_dir" type="str | None" default="None">
The Hugging Face cache directory. Defaults to None.
</ParamField>

<ParamField path="hf_token" type="str | None" default="None">
Hugging Face token for downloading the model, if needed. Defaults to None.
</ParamField>

<ParamField path="text_field" type="str" default="'text'">
The name of the text field in the input data. Defaults to "text".
</ParamField>

<ParamField path="max_chars" type="int | None" default="None">
Limits the total number of characters that can be fed to the tokenizer.
If None, text will not be truncated. Defaults to None.
</ParamField>

<ParamField path="max_seq_length" type="int | None" default="None">
Limits the total sequence returned by the tokenizer so that it has a maximum length.
If None, the tokenizer's model_max_length is used. Defaults to None.
</ParamField>

<ParamField path="padding_side" type="Literal['left', 'right']" default="'right'">
The side to pad the input tokens. Defaults to "right".
</ParamField>

<ParamField path="sort_by_length" type="bool" default="True">
Whether to sort the input data by the length of the input tokens.
Sorting is encouraged to improve the performance of the inference model. Defaults to True.
</ParamField>

<ParamField path="unk_token" type="bool" default="False">
If True, set the pad_token to the tokenizer's unk_token. Defaults to False.
</ParamField>


<ParamField path="name">
</ParamField>
<Anchor id="nemo_curator-stages-text-models-tokenizer-TokenizerStage-_setup">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.text.models.tokenizer.TokenizerStage._setup(
    local_files_only: bool = True
) -> None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="nemo_curator-stages-text-models-tokenizer-TokenizerStage-inputs">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.text.models.tokenizer.TokenizerStage.inputs() -> tuple[list[str], list[str]]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="nemo_curator-stages-text-models-tokenizer-TokenizerStage-load_cfg">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.text.models.tokenizer.TokenizerStage.load_cfg(
    local_files_only: bool = True
) -> transformers.AutoConfig
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="nemo_curator-stages-text-models-tokenizer-TokenizerStage-outputs">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.text.models.tokenizer.TokenizerStage.outputs() -> tuple[list[str], list[str]]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="nemo_curator-stages-text-models-tokenizer-TokenizerStage-process">

<CodeBlock links={{"nemo_curator.tasks.DocumentBatch":"/nemo-curator/nemo_curator/tasks/document#nemo_curator-tasks-document-DocumentBatch"}} showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.text.models.tokenizer.TokenizerStage.process(
    batch: nemo_curator.tasks.DocumentBatch
) -> nemo_curator.tasks.DocumentBatch
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="nemo_curator-stages-text-models-tokenizer-TokenizerStage-ray_stage_spec">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.text.models.tokenizer.TokenizerStage.ray_stage_spec() -> dict[str, typing.Any]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="nemo_curator-stages-text-models-tokenizer-TokenizerStage-setup">

<CodeBlock links={{"nemo_curator.backends.base.WorkerMetadata":"/nemo-curator/nemo_curator/backends/base#nemo_curator-backends-base-WorkerMetadata"}} showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.text.models.tokenizer.TokenizerStage.setup(
    _: nemo_curator.backends.base.WorkerMetadata | None = None
) -> None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="nemo_curator-stages-text-models-tokenizer-TokenizerStage-setup_on_node">

<CodeBlock links={{"nemo_curator.backends.base.NodeInfo":"/nemo-curator/nemo_curator/backends/base#nemo_curator-backends-base-NodeInfo","nemo_curator.backends.base.WorkerMetadata":"/nemo-curator/nemo_curator/backends/base#nemo_curator-backends-base-WorkerMetadata"}} showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.text.models.tokenizer.TokenizerStage.setup_on_node(
    _node_info: nemo_curator.backends.base.NodeInfo | None = None,
    _worker_metadata: nemo_curator.backends.base.WorkerMetadata = None
) -> None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>
