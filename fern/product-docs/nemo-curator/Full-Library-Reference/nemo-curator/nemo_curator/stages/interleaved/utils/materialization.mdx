---
layout: overview
slug: nemo-curator/nemo_curator/stages/interleaved/utils/materialization
title: nemo_curator.stages.interleaved.utils.materialization
---

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`_ClassifiedRows`](#nemo_curator-stages-interleaved-utils-materialization-_ClassifiedRows) | - |

### Functions

| Name | Description |
|------|-------------|
| [`_build_image_mask`](#nemo_curator-stages-interleaved-utils-materialization-_build_image_mask) | - |
| [`_classify_rows`](#nemo_curator-stages-interleaved-utils-materialization-_classify_rows) | Partition pending image rows into three I/O strategy groups. |
| [`_extract_tiff_frame`](#nemo_curator-stages-interleaved-utils-materialization-_extract_tiff_frame) | Extract a single frame from a multi-frame TIFF, returning it as a single-frame TIFF. |
| [`_fill_direct_read_rows`](#nemo_curator-stages-interleaved-utils-materialization-_fill_direct_read_rows) | Read each direct file once, share bytes across all rows referencing it. |
| [`_fill_materialized_bytes`](#nemo_curator-stages-interleaved-utils-materialization-_fill_materialized_bytes) | - |
| [`_fill_range_read_rows`](#nemo_curator-stages-interleaved-utils-materialization-_fill_range_read_rows) | Batch byte-range reads per path using fs.cat_ranges(), deduplicating identical ranges. |
| [`_fill_tar_extract_rows`](#nemo_curator-stages-interleaved-utils-materialization-_fill_tar_extract_rows) | Open each tar once and extract all needed members sequentially. |
| [`_get_frame_index`](#nemo_curator-stages-interleaved-utils-materialization-_get_frame_index) | - |
| [`_init_materialization_buffers`](#nemo_curator-stages-interleaved-utils-materialization-_init_materialization_buffers) | - |
| [`_read_direct_file`](#nemo_curator-stages-interleaved-utils-materialization-_read_direct_file) | - |
| [`_scatter_range_blobs`](#nemo_curator-stages-interleaved-utils-materialization-_scatter_range_blobs) | Distribute deduplicated range-read results, extracting TIFF frames as needed. |
| [`_task_with_dataframe`](#nemo_curator-stages-interleaved-utils-materialization-_task_with_dataframe) | - |
| [`materialize_task_binary_content`](#nemo_curator-stages-interleaved-utils-materialization-materialize_task_binary_content) | Return a task with image-row binary content materialized from source_ref. |

### Data

[`_TAR_EXTENSIONS`](#nemo_curator-stages-interleaved-utils-materialization-_TAR_EXTENSIONS)

### API

<Anchor id="nemo_curator-stages-interleaved-utils-materialization-_ClassifiedRows">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class nemo_curator.stages.interleaved.utils.materialization._ClassifiedRows()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** `NamedTuple`

<ParamField path="direct_read" type="dict[str, list[int]]">
</ParamField>

<ParamField path="missing" type="list[int]">
</ParamField>

<ParamField path="range_read" type="dict[str, list[tuple[int, str, int, int, int | None]]]">
</ParamField>

<ParamField path="tar_extract" type="dict[str, list[tuple[int, str, int | None]]]">
</ParamField>
</Indent>

<Anchor id="nemo_curator-stages-interleaved-utils-materialization-_build_image_mask">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.interleaved.utils.materialization._build_image_mask(
    df: pandas.DataFrame,
    only_missing_binary: bool,
    image_content_types: tuple[str, ...] | None
) -> pandas.Series
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>

<Anchor id="nemo_curator-stages-interleaved-utils-materialization-_classify_rows">

<CodeBlock links={{"nemo_curator.stages.interleaved.utils.materialization._ClassifiedRows":"#nemo_curator-stages-interleaved-utils-materialization-_ClassifiedRows"}} showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.interleaved.utils.materialization._classify_rows(
    df: pandas.DataFrame,
    image_mask: pandas.Series
) -> nemo_curator.stages.interleaved.utils.materialization._ClassifiedRows
```

</CodeBlock>
</Anchor>

<Indent>

Partition pending image rows into three I/O strategy groups.

- tar_extract: has member name but no byte_offset (must open tar and extractfile)
- range_read: has member + byte_offset + byte_size (can use fs.cat_ranges)
- direct_read: no member (path is the file itself)
- missing: path is None/NaN


</Indent>

<Anchor id="nemo_curator-stages-interleaved-utils-materialization-_extract_tiff_frame">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.interleaved.utils.materialization._extract_tiff_frame(
    tiff_bytes: bytes,
    frame_index: int
) -> bytes | None
```

</CodeBlock>
</Anchor>

<Indent>

Extract a single frame from a multi-frame TIFF, returning it as a single-frame TIFF.

Returns the raw bytes unchanged if the data is not a TIFF.


</Indent>

<Anchor id="nemo_curator-stages-interleaved-utils-materialization-_fill_direct_read_rows">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.interleaved.utils.materialization._fill_direct_read_rows(
    groups: dict[str, list[int]],
    storage_options: dict[str, object],
    binary_values: list[object],
    error_values: list[str | None]
) -> None
```

</CodeBlock>
</Anchor>

<Indent>

Read each direct file once, share bytes across all rows referencing it.


</Indent>

<Anchor id="nemo_curator-stages-interleaved-utils-materialization-_fill_materialized_bytes">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.interleaved.utils.materialization._fill_materialized_bytes(
    df: pandas.DataFrame,
    image_mask: pandas.Series,
    storage_options: dict[str, object],
    binary_values: list[object],
    error_values: list[str | None]
) -> None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>

<Anchor id="nemo_curator-stages-interleaved-utils-materialization-_fill_range_read_rows">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.interleaved.utils.materialization._fill_range_read_rows(
    groups: dict[str, list[tuple[int, str, int, int, int | None]]],
    storage_options: dict[str, object],
    binary_values: list[object],
    error_values: list[str | None]
) -> None
```

</CodeBlock>
</Anchor>

<Indent>

Batch byte-range reads per path using fs.cat_ranges(), deduplicating identical ranges.


</Indent>

<Anchor id="nemo_curator-stages-interleaved-utils-materialization-_fill_tar_extract_rows">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.interleaved.utils.materialization._fill_tar_extract_rows(
    groups: dict[str, list[tuple[int, str, int | None]]],
    storage_options: dict[str, object],
    binary_values: list[object],
    error_values: list[str | None]
) -> None
```

</CodeBlock>
</Anchor>

<Indent>

Open each tar once and extract all needed members sequentially.


</Indent>

<Anchor id="nemo_curator-stages-interleaved-utils-materialization-_get_frame_index">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.interleaved.utils.materialization._get_frame_index(
    df: pandas.DataFrame,
    idx: int
) -> int | None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>

<Anchor id="nemo_curator-stages-interleaved-utils-materialization-_init_materialization_buffers">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.interleaved.utils.materialization._init_materialization_buffers(
    df: pandas.DataFrame
) -> tuple[list[object], list[str | None]]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>

<Anchor id="nemo_curator-stages-interleaved-utils-materialization-_read_direct_file">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.interleaved.utils.materialization._read_direct_file(
    path: str,
    storage_options: dict[str, object]
) -> bytes | None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>

<Anchor id="nemo_curator-stages-interleaved-utils-materialization-_scatter_range_blobs">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.interleaved.utils.materialization._scatter_range_blobs(
    blobs: list[object],
    range_keys: list[tuple[int, int]],
    unique_ranges: dict[tuple[int, int], list[tuple[int, str, int | None]]],
    binary_values: list[object],
    error_values: list[str | None]
) -> None
```

</CodeBlock>
</Anchor>

<Indent>

Distribute deduplicated range-read results, extracting TIFF frames as needed.


</Indent>

<Anchor id="nemo_curator-stages-interleaved-utils-materialization-_task_with_dataframe">

<CodeBlock links={{"nemo_curator.tasks.InterleavedBatch":"/nemo-curator/nemo_curator/tasks/interleaved#nemo_curator-tasks-interleaved-InterleavedBatch"}} showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.interleaved.utils.materialization._task_with_dataframe(
    task: nemo_curator.tasks.InterleavedBatch,
    df: pandas.DataFrame
) -> nemo_curator.tasks.InterleavedBatch
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>

<Anchor id="nemo_curator-stages-interleaved-utils-materialization-materialize_task_binary_content">

<CodeBlock links={{"nemo_curator.tasks.InterleavedBatch":"/nemo-curator/nemo_curator/tasks/interleaved#nemo_curator-tasks-interleaved-InterleavedBatch"}} showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.interleaved.utils.materialization.materialize_task_binary_content(
    task: nemo_curator.tasks.InterleavedBatch,
    io_kwargs: dict[str, object] | None = None,
    only_missing_binary: bool = True,
    image_content_types: tuple[str, ...] | None = None
) -> nemo_curator.tasks.InterleavedBatch
```

</CodeBlock>
</Anchor>

<Indent>

Return a task with image-row binary content materialized from source_ref.

Dispatches to three I/O strategies based on source_ref contents:
- range_read: byte_offset + byte_size present -&gt; batched fs.cat_ranges()
- tar_extract: member present, no byte range -&gt; open tar + extractfile
- direct_read: no member -&gt; read file directly


</Indent>

<Anchor id="nemo_curator-stages-interleaved-utils-materialization-_TAR_EXTENSIONS">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.interleaved.utils.materialization._TAR_EXTENSIONS = ('.tar', '.tar.gz', '.tgz')
```

</CodeBlock>
</Anchor>

