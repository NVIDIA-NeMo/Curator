---
layout: overview
slug: nemo-curator/nemo_curator/stages/interleaved/io/writers/base
title: nemo_curator.stages.interleaved.io.writers.base
---

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`BaseInterleavedWriter`](#nemo_curator-stages-interleaved-io-writers-base-BaseInterleavedWriter) | Base class for interleaved writers. |

### API

<Anchor id="nemo_curator-stages-interleaved-io-writers-base-BaseInterleavedWriter">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class nemo_curator.stages.interleaved.io.writers.base.BaseInterleavedWriter(
    path: str,
    file_extension: str,
    write_kwargs: dict[str, typing.Any] = dict(),
    materialize_on_write: bool = True,
    name: str = 'base_interleaved_writer',
    mode: typing.Literal['ignore', 'overwrite', 'append', 'error'] = 'ignore',
    append_mode_implemented: bool = False
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge> <Badge>Abstract</Badge>

**Bases:** [ProcessingStage[InterleavedBatch, FileGroupTask]](/nemo-curator/nemo_curator/stages/base#nemo_curator-stages-base-ProcessingStage)

Base class for interleaved writers.

Handles filesystem setup, deterministic file naming, optional binary
materialization, and process() orchestration.  Subclasses implement
``_write_dataframe`` for format-specific output.


<ParamField path="append_mode_implemented" type="bool = False">
</ParamField>

<ParamField path="file_extension" type="str">
</ParamField>

<ParamField path="materialize_on_write" type="bool = True">
</ParamField>

<ParamField path="mode" type="Literal['ignore', 'overwrite', 'append', 'error'] = 'ignore'">
</ParamField>

<ParamField path="name" type="str = 'base_interleaved_writer'">
</ParamField>

<ParamField path="path" type="str">
</ParamField>

<ParamField path="write_kwargs" type="dict[str, Any] = field(default_factory=dict)">
</ParamField>
<Anchor id="nemo_curator-stages-interleaved-io-writers-base-BaseInterleavedWriter-__post_init__">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.interleaved.io.writers.base.BaseInterleavedWriter.__post_init__() -> None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="nemo_curator-stages-interleaved-io-writers-base-BaseInterleavedWriter-_materialize_dataframe">

<CodeBlock links={{"nemo_curator.tasks.InterleavedBatch":"/nemo-curator/nemo_curator/tasks/interleaved#nemo_curator-tasks-interleaved-InterleavedBatch"}} showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.interleaved.io.writers.base.BaseInterleavedWriter._materialize_dataframe(
    task: nemo_curator.tasks.InterleavedBatch
) -> pandas.DataFrame
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="nemo_curator-stages-interleaved-io-writers-base-BaseInterleavedWriter-_write_dataframe">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.interleaved.io.writers.base.BaseInterleavedWriter._write_dataframe(
    df: pandas.DataFrame,
    file_path: str,
    write_kwargs: dict[str, typing.Any]
) -> None
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>abstract</Badge>

Format-specific DataFrame writer. Subclasses implement this.


</Indent>
<Anchor id="nemo_curator-stages-interleaved-io-writers-base-BaseInterleavedWriter-inputs">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.interleaved.io.writers.base.BaseInterleavedWriter.inputs() -> tuple[list[str], list[str]]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="nemo_curator-stages-interleaved-io-writers-base-BaseInterleavedWriter-outputs">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.interleaved.io.writers.base.BaseInterleavedWriter.outputs() -> tuple[list[str], list[str]]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="nemo_curator-stages-interleaved-io-writers-base-BaseInterleavedWriter-process">

<CodeBlock links={{"nemo_curator.tasks.InterleavedBatch":"/nemo-curator/nemo_curator/tasks/interleaved#nemo_curator-tasks-interleaved-InterleavedBatch","nemo_curator.tasks.FileGroupTask":"/nemo-curator/nemo_curator/tasks/file_group#nemo_curator-tasks-file_group-FileGroupTask"}} showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.interleaved.io.writers.base.BaseInterleavedWriter.process(
    task: nemo_curator.tasks.InterleavedBatch
) -> nemo_curator.tasks.FileGroupTask
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="nemo_curator-stages-interleaved-io-writers-base-BaseInterleavedWriter-write_data">

<CodeBlock links={{"nemo_curator.tasks.InterleavedBatch":"/nemo-curator/nemo_curator/tasks/interleaved#nemo_curator-tasks-interleaved-InterleavedBatch"}} showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.interleaved.io.writers.base.BaseInterleavedWriter.write_data(
    task: nemo_curator.tasks.InterleavedBatch,
    file_path: str
) -> None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>
