---
layout: overview
slug: nemo-curator/nemo_curator/stages/deduplication/io_utils
title: nemo_curator.stages.deduplication.io_utils
---

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`DeduplicationIO`](#nemo_curator-stages-deduplication-io_utils-DeduplicationIO) | - |

### API

<Anchor id="nemo_curator-stages-deduplication-io_utils-DeduplicationIO">

<CodeBlock links={{"nemo_curator.stages.deduplication.id_generator.IdGenerator":"/nemo-curator/nemo_curator/stages/deduplication/id_generator#nemo_curator-stages-deduplication-id_generator-IdGenerator"}} showLineNumbers={false} wordWrap={true}>

```python
class nemo_curator.stages.deduplication.io_utils.DeduplicationIO(
    id_generator: nemo_curator.stages.deduplication.id_generator.IdGenerator | None,
    kwargs = {}
)
```

</CodeBlock>
</Anchor>

<Indent>

<Anchor id="nemo_curator-stages-deduplication-io_utils-DeduplicationIO-assign_id">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.deduplication.io_utils.DeduplicationIO.assign_id(
    filepath: str | list[str],
    df: cudf.DataFrame
) -> cudf.DataFrame
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="nemo_curator-stages-deduplication-io_utils-DeduplicationIO-custom_read">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.deduplication.io_utils.DeduplicationIO.custom_read(
    filepath: str | list[str],
    read_func: collections.abc.Callable,
    assign_id: bool = False,
    kwargs = {}
) -> cudf.DataFrame
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="nemo_curator-stages-deduplication-io_utils-DeduplicationIO-read_jsonl">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.deduplication.io_utils.DeduplicationIO.read_jsonl(
    filepath: str | list[str],
    columns: list[str] | None = None,
    assign_id: bool = False,
    kwargs = {}
) -> cudf.DataFrame
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="nemo_curator-stages-deduplication-io_utils-DeduplicationIO-read_parquet">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.deduplication.io_utils.DeduplicationIO.read_parquet(
    filepath: str | list[str],
    assign_id: bool = False,
    kwargs = {}
) -> cudf.DataFrame
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="nemo_curator-stages-deduplication-io_utils-DeduplicationIO-write_parquet">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.deduplication.io_utils.DeduplicationIO.write_parquet(
    df: cudf.DataFrame,
    filepath: str,
    kwargs = {}
) -> None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>
