---
layout: overview
slug: nemo-curator/nemo_curator/stages/audio/common
title: nemo_curator.stages.audio.common
---

## Module Contents

### Classes

| Name | Description |
|------|-------------|
| [`GetAudioDurationStage`](#nemo_curator-stages-audio-common-GetAudioDurationStage) | Stage that computes the duration of the file in ``audio_filepath_key`` (using soundfile) |
| [`LegacySpeechStage`](#nemo_curator-stages-audio-common-LegacySpeechStage) | LegacySpeechStage for SDP processors inherited from BaseParallelProcessor |
| [`PreserveByValueStage`](#nemo_curator-stages-audio-common-PreserveByValueStage) | Processor for preserving dataset entries based on a specified condition involving a target value and an input field. |

### API

<Anchor id="nemo_curator-stages-audio-common-GetAudioDurationStage">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class nemo_curator.stages.audio.common.GetAudioDurationStage(
    audio_filepath_key: str,
    duration_key: str
)
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>Dataclass</Badge>

**Bases:** [LegacySpeechStage](#nemo_curator-stages-audio-common-LegacySpeechStage)

Stage that computes the duration of the file in ``audio_filepath_key`` (using soundfile)
and saves the duration in ``duration_key``. If there is an error computing the duration,
the value at ``duration_key`` will be updated with the value -1.0.

Returns:
    All the same fields as in the input manifest plus duration_key

**Parameters:**

<ParamField path="audio_filepath_key" type="str">
Key to get path to wav file.
</ParamField>

<ParamField path="duration_key" type="str">
Key to put to audio duration.
</ParamField>


<ParamField path="audio_filepath_key" type="str">
</ParamField>

<ParamField path="duration_key" type="str">
</ParamField>

<ParamField path="name" type="= 'GetAudioDurationStage'">
</ParamField>
<Anchor id="nemo_curator-stages-audio-common-GetAudioDurationStage-process_dataset_entry">

<CodeBlock links={{"nemo_curator.tasks.AudioBatch":"/nemo-curator/nemo_curator/tasks/audio_batch#nemo_curator-tasks-audio_batch-AudioBatch"}} showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.audio.common.GetAudioDurationStage.process_dataset_entry(
    data_entry: dict
) -> list[nemo_curator.tasks.AudioBatch]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="nemo_curator-stages-audio-common-GetAudioDurationStage-setup">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.audio.common.GetAudioDurationStage.setup(
    worker_metadata: typing.Any = None
) -> None
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>

<Anchor id="nemo_curator-stages-audio-common-LegacySpeechStage">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class nemo_curator.stages.audio.common.LegacySpeechStage()
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [ProcessingStage[Task, Task]](/nemo-curator/nemo_curator/stages/base#nemo_curator-stages-base-ProcessingStage)

LegacySpeechStage for SDP processors inherited from BaseParallelProcessor


<Anchor id="nemo_curator-stages-audio-common-LegacySpeechStage-process">

<CodeBlock links={{"nemo_curator.tasks.AudioBatch":"/nemo-curator/nemo_curator/tasks/audio_batch#nemo_curator-tasks-audio_batch-AudioBatch","nemo_curator.tasks.Task":"/nemo-curator/nemo_curator/tasks/tasks#nemo_curator-tasks-tasks-Task"}} showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.audio.common.LegacySpeechStage.process(
    task: nemo_curator.tasks.AudioBatch
) -> list[nemo_curator.tasks.Task]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
<Anchor id="nemo_curator-stages-audio-common-LegacySpeechStage-process_dataset_entry">

<CodeBlock links={{"nemo_curator.tasks.AudioBatch":"/nemo-curator/nemo_curator/tasks/audio_batch#nemo_curator-tasks-audio_batch-AudioBatch"}} showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.audio.common.LegacySpeechStage.process_dataset_entry(
    data_entry: nemo_curator.tasks.AudioBatch
) -> list[nemo_curator.tasks.AudioBatch]
```

</CodeBlock>
</Anchor>

<Indent>

<Badge>abstract</Badge>


</Indent>
</Indent>

<Anchor id="nemo_curator-stages-audio-common-PreserveByValueStage">

<CodeBlock showLineNumbers={false} wordWrap={true}>

```python
class nemo_curator.stages.audio.common.PreserveByValueStage(
    input_value_key: str,
    target_value: int | str,
    operator: str = 'eq'
)
```

</CodeBlock>
</Anchor>

<Indent>

**Bases:** [LegacySpeechStage](#nemo_curator-stages-audio-common-LegacySpeechStage)

Processor for preserving dataset entries based on a specified condition involving a target value and an input field.

**Parameters:**

<ParamField path="input_value_key" type="str">
The field in the dataset entries to be evaluated.
</ParamField>

<ParamField path="target_value" type="Union[int, str]">
The value to compare with the input field.
</ParamField>

<ParamField path="operator" type="str" default="'eq'">
(Optional) The operator to apply for comparison. Options: "lt" (less than), "le" (less than or equal to), "eq" (equal to), "ne" (not equal to), "ge" (greater than or equal to), "gt" (greater than). Defaults to "eq".
</ParamField>

<ParamField path="**kwargs">
Additional keyword arguments to be passed to the base class `BaseParallelProcessor`.
</ParamField>


<ParamField path="name" type="= 'PreserveByValueStage'">
</ParamField>
<Anchor id="nemo_curator-stages-audio-common-PreserveByValueStage-process_dataset_entry">

<CodeBlock links={{"nemo_curator.tasks.AudioBatch":"/nemo-curator/nemo_curator/tasks/audio_batch#nemo_curator-tasks-audio_batch-AudioBatch"}} showLineNumbers={false} wordWrap={true}>

```python
nemo_curator.stages.audio.common.PreserveByValueStage.process_dataset_entry(
    data_entry: nemo_curator.tasks.AudioBatch
) -> list[nemo_curator.tasks.AudioBatch]
```

</CodeBlock>
</Anchor>

<Indent>


</Indent>
</Indent>
